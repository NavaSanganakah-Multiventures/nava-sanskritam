let s1 = "संख्याः";
console.log("s1 ends with ः:", s1.endsWith("ः"), "base:", s1.slice(0, -1));
let s2 = "संख्याम्";
console.log("s2 ends with म्:", s2.endsWith("म्"), "base:", s2.slice(0, -2));
let s3 = "संख्ये";
console.log("s3 ends with े:", s3.endsWith("े"), "base:", s3.slice(0, -1));
