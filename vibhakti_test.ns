नित्य संख्याः = १०;

अस्ति ख = ५;
ख = 55555;

विधिः योग(क, ख) {
    यदि (क > ख) तर्हि {
        फलम् क;
    } अन्यथा {
        फलम् ख;
    }
}

वद(संख्याम्);
वद(योग(संख्याः, ख));
वद(गणनम्(संख्याम्, ख));
वद(समयः());
